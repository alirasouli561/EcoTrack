# ============================================================================
# .DOCKERIGNORE POUR SERVICE-USERS
# ============================================================================
#
# Fichiers/dossiers à EXCLURE lors du "COPY . ." dans le Dockerfile
# Réduit le contexte Docker (plus rapide à construire)
#
# POURQUOI?
#   - Les dépendances sont déjà dans node_modules (copié depuis builder)
#   - Les logs locaux ne sont pas utiles en image
#   - Les .git, .env, node_modules ralentissent le build
# ============================================================================

# Dépendances (déjà copiées depuis builder stage)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Fichiers de configuration/env (sécurité: pas d'exposer secrets)
.env
.env.local
.env.*.local

# VCS (Git)
.git
.gitignore

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs en local
logs/
*.log

# Tests et couverture (on teste en CI/CD, pas en image)
__tests__/
.jest_cache/
coverage/

# Build/dist temporaires
dist/
build/

# CI/CD
.github/
.gitlab-ci.yml

# Documentation (incluse si utile, sinon ignorer)
README.md
docs/

# Scripts de dev
scripts/security_smoke_test.ps1
scripts/security_smoke_test.mjs

# Fichiers système
.DS_Store
Thumbs.db
