# ============================================================================
# ECOTRACK - VARIABLES D'ENVIRONNEMENT
# ============================================================================
# Copiez ce fichier en .env et modifiez les valeurs selon votre environnement
# cp .env.example .env
# ============================================================================

# ============================================================================
# BASE DE DONNÉES POSTGRESQL (Local Docker)
# ============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecotrack
DB_USER=ecotrack_user
DB_PASSWORD=ecotrack_password

# URL complète (générée automatiquement si non définie)
# DATABASE_URL=postgresql://ecotrack_user:ecotrack_password@localhost:5432/ecotrack

# ============================================================================
# PGADMIN (Interface graphique)
# ============================================================================
PGADMIN_EMAIL=admin@ecotrack.dev
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ============================================================================
# API GATEWAY
# ============================================================================
GATEWAY_PORT=3000
GATEWAY_RATE_WINDOW_MS=60000
GATEWAY_RATE_MAX=100

# URLs des services (utilisées par API Gateway)
USERS_PORT=3010
USERS_SERVICE_URL=http://service-users:3010
CONTAINERS_PORT=3011
CONTAINERS_SERVICE_URL=http://service-containers:3011
ROUTES_SERVICE_URL=http://service-routes:3012
IOT_SERVICE_URL=http://service-iot:3013
GAMIFICATIONS_SERVICE_URL=http://service-gamifications:3014
ANALYTICS_SERVICE_URL=http://service-analytics:3015

# ============================================================================
# SERVICE USERS
# ============================================================================
APP_PORT=3010

# JWT Configuration (CHANGEZ CES VALEURS EN PRODUCTION!)
JWT_SECRET=votre_secret_jwt_super_securise_a_changer
JWT_REFRESH_SECRET=votre_refresh_secret_jwt_super_securise_a_changer
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Sécurité
BCRYPT_ROUNDS=10
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_REQUESTS=100

# ============================================================================
# ENVIRONNEMENT
# ============================================================================
NODE_ENV=development

# ============================================================================
# NEON (Cloud - Optionnel)
# Décommentez pour utiliser Neon au lieu de PostgreSQL local
# ============================================================================
# DATABASE_URL=postgresql://neondb_owner:xxx@ep-xxx.neon.tech/neondb?sslmode=require

# ============================================================================
# REDIS (Cache)
# ============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ============================================================================
# CENTRALIZED LOGGING
# ============================================================================
CENTRAL_LOG_URL=http://api-gateway:3000/api/logs
SERVICE_NAME=service-name
